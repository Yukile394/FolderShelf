<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menuRoot"
    android:layout_width="220dp"
    android:layout_height="wrap_content"
    app:cardCornerRadius="20dp"
    app:cardElevation="10dp"
    app:cardBackgroundColor="?attr/colorSurfaceContainerHigh">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingVertical="6dp">

        <LinearLayout
            android:id="@+id/menuSelectFile"
            style="@style/OverlayMenuRow">
            <ImageView style="@style/OverlayMenuIcon" android:src="@drawable/ic_file" />
            <TextView style="@style/OverlayMenuLabel" android:text="@string/menu_select_file" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/menuSelectFolder"
            style="@style/OverlayMenuRow">
            <ImageView style="@style/OverlayMenuIcon" android:src="@drawable/ic_folder" />
            <TextView style="@style/OverlayMenuLabel" android:text="@string/menu_select_folder" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/menuRecent"
            style="@style/OverlayMenuRow">
            <ImageView style="@style/OverlayMenuIcon" android:src="@drawable/ic_history" />
            <TextView style="@style/OverlayMenuLabel" android:text="@string/menu_recent" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/menuSettings"
            style="@style/OverlayMenuRow">
            <ImageView style="@style/OverlayMenuIcon" android:src="@drawable/ic_settings" />
            <TextView style="@style/OverlayMenuLabel" android:text="@string/menu_settings" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/menuClose"
            style="@style/OverlayMenuRow">
            <ImageView style="@style/OverlayMenuIcon" android:src="@drawable/ic_close" />
            <TextView style="@style/OverlayMenuLabel" android:text="@string/menu_close" />
        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
